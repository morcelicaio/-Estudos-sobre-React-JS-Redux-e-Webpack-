// Arquivo de constantes que serão os endpoints (rotas) que serão a base da url dos web services.

    // API_URL  são os endpoints que estão protegidos   
    // OAPI_URL são os endpoints que estão abertos.
export default {    
    API_URL: 'http://localhost:3004/api',
    OAPI_URL: 'http://localhost:3004/oapi',
}